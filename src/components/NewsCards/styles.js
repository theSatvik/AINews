import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles({
  container: {
    padding: "0 5%",
    width: "100%",
    margin: 0,
  },
  headings: {
    fontWeight: "bolder",
    fontSize: "50px",
  },
});

export default styles;
